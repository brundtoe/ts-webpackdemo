document.addEventListener('DOMContentLoaded', function(e) {
    const el: null | HTMLElement = document.getElementById('data')
        if (el) {
            el.innerHTML = '<strong>Hello World</strong>'
        }
})
